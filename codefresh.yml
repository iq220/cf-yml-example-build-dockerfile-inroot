version: '1.0'

steps:

  build_prj:
    type: build
    description: Build an Image with the Dockerfile in Root Directory
    image_name: codefreshio/Thooth
    dockerfile: Dockerfile
    tag: ${{CF_BRANCH}}
